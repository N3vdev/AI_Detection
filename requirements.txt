# ── PyTorch with CUDA 12.6 (compatible with CUDA 12.x and 13.x drivers) ────────
# RTX 4060 / 4070 / 4080 / 4090 and any GPU with CUDA 13.x driver
--extra-index-url https://download.pytorch.org/whl/cu126
torch
torchvision
accelerate

# ── Vision-Language Model ─────────────────────────────────────────────────────
# Qwen2.5-VL-7B-Instruct (~15 GB, auto-downloaded on first run)
# Runs in 4-bit on RTX 4060 12GB (~5 GB VRAM used)
transformers>=4.51.0
huggingface-hub
sentencepiece
tiktoken

# 4-bit quantization — auto-enabled when free VRAM < 12 GB
bitsandbytes

# Optional: Flash Attention 2 for 2-4x faster inference (Ada/Ampere GPUs)
# pip install flash-attn --no-build-isolation

# ── Computer Vision ───────────────────────────────────────────────────────────
# opencv-contrib required for cv2.barcode.BarcodeDetector
opencv-contrib-python
Pillow
numpy

# ── Barcode Detection ─────────────────────────────────────────────────────────
# YOLO barcode region detector (models/barcode_detector.pt)
ultralytics
# pyzbar on Windows requires ZBar DLL: https://pypi.org/project/pyzbar/
pyzbar

# ── OCR ───────────────────────────────────────────────────────────────────────
# PaddleOCR replaces custom CRNN — PP-OCRv4 handles inkjet/dotted labels reliably
#
# Step 1 — install PaddlePaddle GPU (CUDA 12.6, compatible with CUDA 13.x drivers):
#   pip install paddlepaddle-gpu -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# If Python 3.13 wheel is unavailable, fall back to CPU:
#   pip install paddlepaddle
#
# Step 2 — install PaddleOCR:
#   pip install paddleocr
paddleocr

# ── Utilities ─────────────────────────────────────────────────────────────────
pyyaml
